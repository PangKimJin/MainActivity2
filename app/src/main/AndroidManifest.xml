<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mainactivity">
    <!-- Camera and Storage Permissions Required -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".EditExpenditureListLongHold"></activity>
        <activity android:name=".DashboardBarchart" />
        <activity android:name=".DashboardSlide" />
        <activity android:name=".Dashboard" />
        <activity android:name=".EditExpenditureList" />
        <activity android:name=".ExpenditureItemDisplay" />
        <activity android:name=".ExpenditureListCreateItem" />
        <activity android:name=".ExpenditureListDisplay" />
        <activity android:name=".CreateNewExpenditureList" />
        <activity android:name=".MyPastExpenditure" />
        <activity android:name=".ItemDisplay" />
        <activity android:name=".Create_New_List" />
        <activity android:name=".ITTR" />
        <activity
            android:name=".CreateLists"
            android:exported="true" />
        <activity android:name=".shopping_list_display" />
        <activity android:name=".UserInputForm" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- add in application tag -->
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/AppTheme" />
    </application>

</manifest>